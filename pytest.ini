[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
addopts = --cov=two_fast_auth --cov-report=term-missing
asyncio_default_fixture_loop_scope = function
markers =
    asyncio: mark tests as async